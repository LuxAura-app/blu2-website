.DS_Store
Thumbs.db
node_modules/
.env
.env.local
*.log
desktop.ini

# Source audio for the story video (large; already embedded in the mp4)
Audio/

# Claude local memory (not part of the site)
memory/

# Stray nested working copy (not part of the site)
BLU2/

.vercel
.env*

# Admin data pulls (real subscriber PII — local only, never committed)
admin-exports/
