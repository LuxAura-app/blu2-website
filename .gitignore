.DS_Store
Thumbs.db
node_modules/
.env
.env.local
*.log
desktop.ini

# Local-only tooling for rendering the story video (ffmpeg-static, etc.)
# Kept out of git so it doesn't alter the Vercel static build.
package.json
package-lock.json

# Source audio for the story video (large; already embedded in the mp4)
Audio/

# Claude local memory (not part of the site)
memory/

# Stray nested working copy (not part of the site)
BLU2/
